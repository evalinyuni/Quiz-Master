
<%= form_for(@exam) do |f| %>
  <% if @exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exam.errors.count, "error") %> prohibited this question from being saved:</h2>

      <ul>
      <% @exam.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br />
    <%= f.text_area :question, :size => "50x3", :required => true%>
  </div>

  <div class="field">
    <%= f.label :answer %><br />
    <%= f.text_area :answer, :size => "50x3", :required => true%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
